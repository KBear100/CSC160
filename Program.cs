using System;
using System.Numerics;
using ConsoleLibrary;

namespace Fountation
{
    class Program
    {
        static void Main(string[] args)
        {
            //int result = IO.GetConsoleInt("Enter a number between 5 and 10: ", 5, 10);
            //IO.Print(result.ToString());

            //float test = IO.GetConsoleFloat("Enter Float between 1 and 2: ", 1, 2);
            //IO.Print(test.ToString());

            //bool btest = IO.GetConsoleBool("Enter Bool");
            //IO.Print(btest.ToString());

            //char ctest = IO.GetConsoleChar("Enter Char");
            //IO.Print(ctest.ToString());

            //string sTest = IO.GetConsoleString("Enter String");
            //IO.Print(sTest);

            //string[] menu = { "Attack", "Denfend", "Items" };
            //int menuTest = IO.GetConsoleMenu(menu);
            //IO.Print(menuTest.ToString());

            //Console.WriteLine("Hello World!");
            //Console.WriteLine("What is your name?");
            //string strName = Console.ReadLine();
            //Console.WriteLine("Hello " + strName);
            //Console.WriteLine("Hello {0} go die!", strName);
            //Console.WriteLine($"Hello again {strName}, please go away.");

            //DataTypes.Execute();

            //Converting.DoIt();

            //ValueReferencecs.Engage();

            //Parameters.DoIt();

            //Strings.Start();

            //Arrays.Go();

            //Inheritance.yes();

            //Generics.Go();

            ExtensionMethods.Go();
        }
    }
}